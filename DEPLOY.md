# Phase 1 — 배포하고 확인하기

> **이 단계의 목적은 딱 하나입니다.**
> Claude Pro 구독 로그인이 프록시를 하나 거쳐도 통과하는지 확인하는 것.
> 여기서 막히면 이후 작업이 전부 무의미하므로, **NVIDIA 코드는 일부러 넣지 않았습니다.**

소요 시간: **10~20분**

---

## 먼저: 제가 못 해드리는 두 가지

솔직하게 말씀드립니다. 이 단계는 제가 대신 해드릴 수 없는 부분이 있습니다.

**1. 배포는 직접 하셔야 합니다.**
배포하려면 Cloudflare 계정 열쇠가 필요한데, 그걸 이 대화창에 붙여넣으면 **대화 기록에 영구히 남습니다.** 아래 **A안**은 열쇠를 아무 데도 넘기지 않고 배포하는 방법입니다. 그쪽을 권합니다.

**2. 최종 확인도 직접 하셔야 합니다.**
확인하려면 **당신의 Pro 구독 로그인**으로 Claude Code를 실행해야 합니다. 그 로그인 정보는 당신 기기에만 있고, 저에게는 없습니다. 애초에 저한테 있으면 안 되는 것이기도 합니다.

즉 **코드는 제가, 버튼은 당신이** 누르는 구조입니다.

---

## A안. 대시보드에 붙여넣기 (권장 · 설치 불필요)

컴퓨터에 아무것도 설치하지 않아도 되고, 휴대폰 브라우저로도 됩니다.

### 1. 코드 복사하기

[`src/index.js` 파일 열기](https://github.com/dream8837z/Extra-Booster/blob/claude/mobile-nvidia-ai-token-handling-uaybdg/src/index.js) → 오른쪽 위 **복사 아이콘(⧉)** 클릭

> 내용을 읽을 필요 없습니다. 통째로 복사만 하면 됩니다.

### 2. Worker 만들기

1. [dash.cloudflare.com](https://dash.cloudflare.com) 로그인
2. 왼쪽 메뉴 **Compute (Workers)** 또는 **Workers & Pages**
3. **Create** → **Start with Hello World!** → **Get started**
4. 이름을 `extra-booster` 로 바꾸고 **Deploy**

여기까지 하면 `https://extra-booster.<계정이름>.workers.dev` 같은 주소가 생깁니다.

### 3. 코드 바꿔치기

1. **Edit code** (또는 **< > Edit code**) 클릭
2. 편집기에 있던 기존 내용 **전부 지우기** (`Ctrl+A` → `Delete`)
3. 1번에서 복사한 코드 **붙여넣기**
4. 오른쪽 위 **Deploy** → **Save and deploy**

### 4. 잘 올라갔는지 보기

브라우저로 아까 그 주소를 열어보세요.

**✅ Extra-Booster 작동 중** 이라는 화면이 나오면 배포 성공입니다.

> 화면이 안 나오고 오류가 나면, 오류 메시지를 그대로 복사해서 저한테 보내주세요.

---

## B안. 명령어로 배포 (PC에 개발 도구가 있는 경우)

```bash
git clone https://github.com/dream8837z/Extra-Booster.git
cd Extra-Booster
git checkout claude/mobile-nvidia-ai-token-handling-uaybdg

npx wrangler login     # 브라우저가 열리고 로그인 → 열쇠를 손으로 다룰 일 없음
npx wrangler deploy
```

마지막 줄에 배포된 주소가 찍힙니다.

---

## ⭐ 진짜 확인 — 여기가 Phase 1의 본론입니다

배포 성공 화면이 나온 건 **"서버가 살아 있다"**까지만 증명합니다.
아직 **"Pro 구독 로그인이 이걸 통과한다"**는 증명되지 않았습니다.

### 왜 눈으로만 봐선 안 되나요?

만약 Claude Code가 `ANTHROPIC_BASE_URL` 설정을 **그냥 무시**해버리면,
Claude Code는 평소처럼 잘 동작합니다. 그럼 당신은 **"오, 되네!"** 하고 착각하게 됩니다.
실제로는 프록시를 안 거치고 있는데도요.

그래서 **로그에 실제로 요청이 찍히는지**를 봐야 합니다. 이게 유일한 증거입니다.

### 1) 로그 창 먼저 켜기

**터미널이 있다면:**
```bash
npx wrangler tail --format pretty
```

**터미널이 없다면 (휴대폰 포함):**
Cloudflare 대시보드 → 방금 만든 **extra-booster** Worker → **Logs** 탭 → **Live** / **실시간 로그 시작**

이 창을 켜 둔 채로 다음 단계를 하세요.

### 2) 주소 연결하기

**Mac / Linux 터미널**
```bash
export ANTHROPIC_BASE_URL="https://extra-booster.계정이름.workers.dev"
claude
```

**Windows PowerShell**
```powershell
$env:ANTHROPIC_BASE_URL="https://extra-booster.계정이름.workers.dev"
claude
```

**Claude Code 웹/데스크톱 앱을 쓰신다면**
환경 설정에서 환경변수를 지정하는 자리가 있습니다. 찾기 어려우면 저에게 물어보세요.
가장 확실하게 확인되는 곳은 **PC의 터미널**입니다.

### 3) 아무거나 물어보기

Claude Code에서 `안녕` 같은 걸 물어보세요.

### 4) 로그 창 확인 ← **판정**

로그 창에 이런 줄이 뜨는지 보세요.

```json
{"at":"2026-08-09T...","path":"/v1/messages","method":"POST","status":200,"ms":842,"auth":"oauth"}
```

여기서 **두 군데**가 핵심입니다.

| 항목 | 의미 |
|---|---|
| `"path":"/v1/messages"` | 요청이 **실제로 프록시를 거쳤다** |
| `"auth":"oauth"` | **Pro 구독 로그인 방식**으로 통과했다 ← 우리가 알고 싶던 바로 그것 |

> `"auth":"api-key"` 로 찍혔다면 구독이 아니라 API 키로 붙은 것입니다. 그것도 동작은 하지만
> Phase 1이 답하려던 질문과는 다르니, 저한테 알려주세요.

---

## 결과별 다음 행동

| 상황 | 판정 | 다음 |
|---|---|---|
| 로그에 줄이 뜨고 Claude가 평소처럼 답함 | ✅ **통과** | Phase 2로. 저한테 "1단계 통과했어"라고 말씀해 주세요 |
| Claude는 잘 되는데 로그가 **텅 비어 있음** | ⚠️ **미확인** | 설정이 무시된 것. 확인된 게 없으니 진행 금지 |
| 로그인 오류 / 인증 실패 / 401 · 403 | ❌ **막힘** | **여기서 중단.** 오류 메시지를 그대로 보내주세요 |
| 답변이 한 번에 뭉쳐서 나옴 (타자 치듯 안 나옴) | ⚠️ **스트리밍 문제** | 동작은 하지만 고쳐야 합니다. 알려주세요 |

❌가 나와도 **손해는 20분**입니다.
안 될 걸 모른 채로 며칠 만드는 것보다 압도적으로 낫습니다. 그게 이 단계를 맨 앞에 둔 이유입니다.

---

## 되돌리기

언제든 원래대로 돌아갈 수 있습니다.

```bash
unset ANTHROPIC_BASE_URL      # Windows: Remove-Item Env:ANTHROPIC_BASE_URL
```

터미널을 새로 열어도 초기화됩니다. Claude Code 자체는 전혀 건드리지 않았습니다.

---

## 이 프록시가 실제로 하는 일

읽지 않으셔도 되지만, 궁금하실까 봐 적어둡니다. 전부 다 해서 이게 답니다.

1. 요청이 오면 → 주소만 `api.anthropic.com` 으로 바꿔서 **그대로** 보냄
2. 응답이 오면 → **그대로** 돌려줌
3. 그 사이에 "언제, 어떤 경로로, 어떤 인증 방식으로, 몇 초 걸렸는지"만 기록

**기록하지 않는 것:** 대화 내용, 로그인 토큰 값, API 키. 한 글자도 남기지 않습니다.

Phase 1 코드에는 **비밀 열쇠가 하나도 들어 있지 않습니다.** 요청자가 자기 인증 정보를 직접 들고 와야 하므로, 주소가 노출돼도 남이 당신 계정을 쓸 수는 없습니다.

> 단, **Phase 2에서 NVIDIA 열쇠를 넣는 순간 이야기가 달라집니다.** 그때는 프록시 자체에
> 자물쇠를 달아야 합니다. 잊지 않도록 `PLAN.md` §9에 적어두었습니다.
